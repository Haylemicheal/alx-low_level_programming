#include "main.h"

/**
 * _abs - Computes the absoulte value of an integer
 * @n: Number param
 * Return: The absoulte value of the input
 */
int _abs(int n)
{
	if (n < 0)
	{
		n = n * -1;
	}
	return (n);
}

